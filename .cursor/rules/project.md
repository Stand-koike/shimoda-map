# このワークスペース = 下田マップ（shimoda-map）

- **リポジトリ**: `Stand-koike/shimoda-map`（本フォルダ `下田MAP`）
- **編集対象**: `web/`（下田設定）, `clients/shimoda/`, `clients/_template/`
- **触らない**: `sotoura-map` / 外浦MAP リポ、`clients/sotoura/` を web に反映しない
- **確認**: `web/config.js` の `APP_TITLE` は「下田マップ」
- **ブランチ**: 本番は **`main`** のみ（`area/sotoura` はアーカイブ）

外浦作業は別 Workspace（`外浦MAP`）で開く。

Agent 依頼の冒頭:

```
【案件】下田 / リポ shimoda-map / main
```
